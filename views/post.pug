extends layout.pug

block body 
  header
    section 
      form(action='/post' method='POST')
        h1
          | Post a message
        textarea#about(name="message", cols="80", rows="5" required) 
        a(href="/")
          i
            | cancel
        button(type="submit") 
          | Send 